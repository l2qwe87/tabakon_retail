
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Строка	=	 РеквизитФормыВЗначение("Объект").ПользователиОзнакомились.Найти(Пользователи.ТекущийПользователь());
	Если Строка = Неопределено тогда
     	НовСтрока				=	 Объект.ПользователиОзнакомились.Добавить();
		НовСтрока.Пользователь	=	Пользователи.ТекущийПользователь();
	КонецЕсли;  
	
КонецПроцедуры
