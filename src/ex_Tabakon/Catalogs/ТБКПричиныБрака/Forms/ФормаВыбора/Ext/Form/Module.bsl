
&НаКлиенте
Процедура Отбор(Команда)
	
	Если Элементы.Отбор.Заголовок = "Сбросить отбор" Тогда
		Для каждого ОТбор Из Список.отбор.Элементы Цикл
			Отбор.Использование  = Ложь;		
		КонецЦикла;	
		Элементы.Отбор.Заголовок = "Включить отбор";
	ИначеЕсли Элементы.Отбор.Заголовок = "Включить отбор" Тогда
		Для каждого ОТбор Из Список.отбор.Элементы Цикл
			Отбор.Использование  = Истина;		
		КонецЦикла;	
		Элементы.Отбор.Заголовок = "Сбросить отбор";	
	КонецЕсли;

КонецПроцедуры
