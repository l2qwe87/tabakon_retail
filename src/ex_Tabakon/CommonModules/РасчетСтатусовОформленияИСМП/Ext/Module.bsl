

&Вместо("ЗаписатьСтатусОформленияДокументов")
Процедура ТБКЗаписатьСтатусОформленияДокументов(МассивДокументов, МетаданныеДокументаИСМП, ТаблицаРеквизитов)
	// Вставить содержимое метода.
	//отключаем запись в регистр Статусы оформления документов ИСМП. Записи в любом случае НЕ делаются, а запросы - очень сильно тупят при каждом пробитии чека
	
	//ПродолжитьВызов(МассивДокументов, МетаданныеДокументаИСМП, ТаблицаРеквизитов);
	
КонецПроцедуры

