
&Перед("ПередЗаписью")
Процедура ТБКПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	//Костыль. Заполнение склада и магазина(при обмене склад объект не найден)
	//Марк 2023-10-06
	Если Магазин.Пустая() или Склад.Пустая() Тогда 		
		Магазин = ОбщегоНазначения.ПолучитьМагазин();
		Склад   = Магазин.СкладПродажи;	
		Ориентация = Склад.Организация;
	КонецЕсли;
	//КонецМ
КонецПроцедуры
