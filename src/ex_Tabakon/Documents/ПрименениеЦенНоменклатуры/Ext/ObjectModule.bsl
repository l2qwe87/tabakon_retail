
&После("ПередЗаписью")
Процедура ТБКПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	Если НЕ РольДоступна("ПолныеПрава") тогда
		Если Найти(Комментарий, "НЕ принимать, будет проведена только после согласования автоматически.")	тогда
			Отказ = Истина;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
